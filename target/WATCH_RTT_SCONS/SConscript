from building import *
Import('rtconfig')

cwd = GetCurrentDir()
src = Glob('*.c')
include_path = os.path.join(cwd,'include')
CPPPATH = [include_path]

group = DefineGroup('shs_src', src, depend=[''], CPPPATH=CPPPATH)

Return('group')

